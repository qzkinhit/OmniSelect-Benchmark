# AAAI numeric-exceptions register (audit 1910)

After the mechanical canonical sync (all stale tokens rewritten from
`experiments/canonical_tables.json`), the following are the ONLY sanctioned
exceptions and secondary sources. Anything numeric in the AAAI tex outside the
canonical tables must trace to one of these entries.

1. **Registered legacy table** — `sections/method.tex` `tab:main-signal` (marked
   in-file with `% REGISTERED LEGACY EXCEPTION (audit 1910)` and a LEGACY caption).
   Its numbers are the pre-paired-batch snapshot kept solely to illustrate the
   signal-flip phenomenon; the experiments section supersedes them.

2. **Extended-view supplementary table is SINGLE-SOURCE per row and machine-bound**
   (audit 2030-4): every cell of the CSTR / steamgen / ETTh2 rows (controller
   included) is the `experiments/results_matrix.json` three-seed mean from ONE
   shared-testbed source log (log_sha256 re-verified), the `full` cell parsed from
   the same log; the ETTh1 row is the paper's FINAL pubcore-paired source. The
   machine-readable binding is `experiments/extended_view_source_ledger.json`
   (generated by `scripts/build_extended_view_ledger.py`, which also asserts the
   LaTeX rows equal the ledger-derived strings). Applied values: CSTR 1.013,
   steamgen 0.858, ETTh2 0.631, ETTm1 prose 1.014/1.057/1.248.
   `controller_current_canonical_v5` (e.g. CSTR 1.009) is an ANALYSIS-LAYER
   re-adjudication namespace used by the characteristic-metrics scripts only; it
   is recorded in the ledger as a non-adopted alternative and never mixed into
   the tables.

3. **Full-data reference values on supplementary views** (CSTR 1.074,
   steamgen 0.995, ETTh2 0.669) have no cell in `canonical_tables.json`; their
   mechanical source is the tracked extended-lane logs
   `experiments/daisy_cstr_3seed.log`, `experiments/daisy_steamgen_3seed.log`,
   `experiments/etth2_3seed.log` (three-seed means of the `full` rows).

4. **`tab:contam` / winner's-curse ablation numbers** (`sections/experiments.tex`,
   e.g. 0.417/0.428 under the split-protocol ablations) belong to those dedicated
   ablation experiments, not to the four-arm main protocol; they intentionally
   differ from main-table cells.

Verification: `grep -rn "0\.974\|1\.026\|1\.084\|1\.288" papers/mmdataselect/submissions/aaai2027/sections/`
must return exactly ONE sanctioned same-token coincidence: `experiments.tex` tab:supp
row `DaISy CSTR & \textbf{1.013} & 1.026 & ...` where 1.026 is the CSTR
authenticity cell (`cells.time_daisy_cstr.auth_only = 1.026$\pm$0.007`), unrelated
to the retired ETTm1 value. Every other numeric literal must match
`canonical_tables.json` or an entry above.

5. **Half-boundary formatting artifact (ETTm1 authenticity)** — the exact three-seed
   mean of `cells.time_ettm1.auth_only` is 1.0575, sitting exactly on the 3-decimal
   half boundary. The frozen canonical fmt string is `1.057$\pm$0.027` (produced by
   the generator interpreter's binary `%.3f`), while decimal ROUND_HALF_UP of the
   exact mean gives 1.058. The manuscripts quote the frozen canonical fmt (1.057);
   the claim-evidence matrix binds this number to the fmt string itself. Canonical
   is frozen and is NOT regenerated over a 0.001 display artifact; a future
   regeneration should switch the formatter to decimal half-up.
